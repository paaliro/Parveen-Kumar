export type FeatureItem = {
  iconPath: string;
  heading: string;
  text: string;
};

export interface WorkContent {
  starPath: string;
  text: string;
  imagePath: string;
}

export interface WorkContent2 {
  header: string;
  date: string;
  list: string[];
}
